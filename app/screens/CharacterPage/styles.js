import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20,
    alignItems: 'center',
    fontSize: 20,
  },
  childContainer: {
    display: 'flex',
    flexDirection: 'row',
  },
  child: {
    padding: 10,
  },
});

export default styles;
